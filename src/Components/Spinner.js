import React from 'react'
import spinner from '../img/cheetah.gif'

const Spinner = () => {
  return (
    <img className='spinner' src={spinner} alt='cheetah running' />
  
  )
}


export default Spinner